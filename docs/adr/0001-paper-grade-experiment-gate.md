# Paper-Grade Experiment Gate

Status: accepted

DAR-TD3BC paper results must come from a full paper experiment, not from smoke runs or offline validation diagnostics. This decision rejects the faster path of treating existing single-seed validation-NPZ metrics as final evidence, because the paper plan requires closed-loop rollout evaluation, official normalized return, multiple training seeds, ablations, robustness tests, statistical aggregation, and complete provenance.
